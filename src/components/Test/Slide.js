import React from 'react'
import { withRouter } from 'react-router-dom'
import './Slide.sass'

const Slide = props => {
  return (
      <div>
        TEST!
      </div>
  )
}

export default withRouter(Slide)
